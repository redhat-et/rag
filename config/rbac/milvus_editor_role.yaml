# permissions for end users to edit milvuses.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: rag
    app.kubernetes.io/managed-by: kustomize
  name: milvus-editor-role
rules:
- apiGroups:
  - rag.opendatahub.io
  resources:
  - milvuses
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - rag.opendatahub.io
  resources:
  - milvuses/status
  verbs:
  - get
